= form_for @saved_search, url: "/#{controller_name}" do |f|
  .field
    = f.label :name, "Search query / reference"
    = f.text_field :name, placeholder: "term", autofocus: true

  .submit
    %button.icon-search3{ type: 'submit' } Search
